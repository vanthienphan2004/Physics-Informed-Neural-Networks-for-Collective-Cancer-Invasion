repos:
-   repo: https://github.com/psf/black
    rev: 24.8.0  # Use the latest stable version or a specific version
    hooks:
    -   id: black
        args: [--line-length=88]  # Optional: customize line length
        files: \.py$  # Only apply to Python files